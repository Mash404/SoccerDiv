@model IEnumerable<SoccerDiv.Models.Venue>

@{
    ViewBag.Title = "Venue";
}

<link href="~/Content/team.css" rel="stylesheet" />
<link href="~/Content/adminbutton.css" rel="stylesheet" />

<div class="containerX">
    <a class="card1" href="@Url.Action("AdminView", "Admins")">
        <h3>Back To Home</h3>
        <div class="go-corner">
            <div class="go-arrow">
                →
            </div>
        </div>
    </a>
    <a class="card1" href="@Url.Action("AddVenue", "Venues")">
        <h3>Add New Venue</h3>
        <div class="go-corner">
            <div class="go-arrow">
                →
            </div>
        </div>
    </a>
</div>

<div class="tableX">
    <table class="containerD" style="width:1140px;">
        <thead>
            <tr>
                <th>
                    <h1 style="color: #3BFD0B; font-family: cursive; font-size: 20px;">
                        @Html.DisplayNameFor(model => model.Venue_Name)
                    </h1>
                </th>
                <th>
                    <h1 style="color: #3BFD0B; font-family: cursive; font-size: 20px;">
                        @Html.DisplayNameFor(model => model.Venue_Capacity)
                    </h1>
                </th>
                <th>
                    <h1 style="color: #3BFD0B; margin-left:40px; font-family: cursive; font-size: 20px;">
                        @Html.DisplayNameFor(model => model.Venue_Location)
                    </h1>
                </th>
                <th>
                    <h1 style="color: #3BFD0B; font-family: cursive; font-size: 20px;">
                        @Html.DisplayNameFor(model => model.Venue_Image)
                    </h1>
                </th>
                <th style="width: 180px;">
                    <h1 style="color: #3BFD0B; font-family:cursive; font-size:20px;">
                        Controll
                    </h1>
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Venue_Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Venue_Capacity)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Venue_Location)
                    </td>
                    <td>
                        @if (item.Venue_Image != null)
                        {
                            var imgPath = item.Venue_Image;
                            <img src="@Url.Content(imgPath)" alt="profile card" style="width:50%; height:50%;">
                        }
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.Venue_ID }) |
                        @Html.ActionLink("Details", "Details", new { id = item.Venue_ID }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.Venue_ID })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>