@model IEnumerable<SoccerDiv.Models.Admin>

@{
    ViewBag.Title = "AdminList";
}

<link href="~/Content/team.css" rel="stylesheet" />
<link href="~/Content/adminbutton.css" rel="stylesheet" />

<div class="containerX">
    <a class="card1" href="@Url.Action("AdminView", "Admins")">
        <h3>Back To Home</h3>
        <div class="go-corner">
            <div class="go-arrow">
                →
            </div>
        </div>
    </a>
    <a class="card1" href="@Url.Action("SignUp", "Admins")">
        <h3>Join New Admin</h3>
        <div class="go-corner">
            <div class="go-arrow">
                →
            </div>
        </div>
    </a>
</div>

<div class="tableX">
    <table class="containerD" style="width:1140px;">
        <thead>
            <tr>
                <th>
                    <h1 style="color: #3BFD0B; font-family: cursive; font-size: 20px;">
                        @Html.DisplayNameFor(model => model.Admin_Name)
                    </h1>
                </th>
                <th>
                    <h1 style="color: #3BFD0B; font-family: cursive; font-size: 20px;">
                        @Html.DisplayNameFor(model => model.Admin_Email)
                    </h1>
                </th>
                <th>
                    <h1 style="color: #3BFD0B; margin-left:40px; font-family: cursive; font-size: 20px;">
                        @Html.DisplayNameFor(model => model.Admin_Image)
                    </h1>
                </th>
                <th>
                    <h1 style="color: #3BFD0B; font-family: cursive; font-size: 20px;">
                        @Html.DisplayNameFor(model => model.Admin_Dexcription)
                    </h1>
                </th>
                <th style="width: 180px;">
                    <h1 style="color: #3BFD0B; font-family:cursive; font-size:20px;">
                        Controll
                    </h1>
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Admin_Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Admin_Email)
                    </td>
                    <td>
                        @if (item.Admin_Image != null)
                        {
                            var imgPath = item.Admin_Image;
                            <img src="@Url.Content(imgPath)" alt="profile card" style="width:10%; height:10%;">
                        }
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Admin_Dexcription)
                    </td>
                    <td>
                        @Html.ActionLink("Details", "Details", new { id = item.Admin_ID }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.Admin_ID })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>